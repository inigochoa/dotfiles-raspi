- defaults:
    link:
        create: true
        relink: true

- clean: ['~']

- link:
    ~/.profile: ~
    ~/.bashrc: ~
    ~/.bash_logout: ~
    ~/.bash_aliases: ~
    ~/.gitconfig: ~
    ~/.scripts/status.sh: scripts/status.sh

- shell:
    - . $HOME/.bashrc
    - source $HOME/.bashrc
